<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>$(DEVELOPMENT_LANGUAGE)</string>
	<key>CFBundleDisplayName</key>
	<string>Roughly</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<!-- Without this key every App Store Connect upload stops to ask the
	     export-compliance question. The app uses only HTTPS, which is exempt. -->
	<!-- Shown verbatim in the permission prompt, so it says what the photo is
	     for rather than restating that the app wants the camera. A vague
	     purpose string is a routine 5.1.1 rejection, and it is also the only
	     explanation the user gets at the moment they decide.
	     The photo library needs no key: PhotosPicker runs out of process and
	     hands back only what the user picked. -->
	<!-- App Intents shortcuts do not request Siri authorisation the way SiriKit
	     did, so this may be unnecessary. Kept because the guidance is mixed,
	     it is one line, and a missing usage string is a rejection while a
	     surplus one is not. -->
	<key>NSSiriUsageDescription</key>
	<string>Log what you ate by talking to Siri instead of opening the app.</string>
	<key>NSCameraUsageDescription</key>
	<string>Take a photo of a meal and the coach will estimate its calories and protein for you.</string>
	<key>ITSAppUsesNonExemptEncryption</key>
	<false/>
	<key>UILaunchScreen</key>
	<dict/>
	<key>UIApplicationSceneManifest</key>
	<dict>
		<key>UIApplicationSupportsMultipleScenes</key>
		<false/>
	</dict>
</dict>
</plist>
